\section{Open Problems and Conclusion}
\label{sec:conclusion}

% Drive the conclusion from the gap-map (§8) and the §2 findings. The open
% problems ARE the empty matrix cells + the side-channel blind spot.
% Candidate open problems to articulate:
%   - obfuscation/DP almost absent from storage & retrieval (most cells empty);
%   - graph-RAG privacy barely addressed by any mechanism;
%   - no scheme defends the side-channel surface its TEE anchor depends on (§2);
%   - cross-stage mitigation composition unstudied (Bodea concurs);
%   - performance is self-reported and non-comparable -> need a common benchmark.
\needswork{synthesize open problems from the gap-map + §2 findings; close with
the SoK's framing contribution}

% Forward pointer (no scooping): the companion paper instantiates one cell ---
% a unified static+dynamic obfuscated-inference design --- motivated by the validation case study.
