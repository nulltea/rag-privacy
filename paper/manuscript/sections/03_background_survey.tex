\section{Background and Survey}
\label{sec:survey}

% LOCKED: organize the survey BY TRUST MECHANISM (classic shape). One subsection
% per family; a tradeoff table; attacks discussed in-line. The adversarial punch
% lives in Secs. 4-6, NOT here — keep this section calm and framework-like.
%
% Coverage target is an OPEN decision (how many papers / sub-areas). Seed corpus:
% the 5 core papers + the EdgeQuake-indexed set. \needswork below.

\dnote{open decision: survey coverage target (paper count + sub-areas) — see
stage-1 decision record, still-open item \#1}

\subsection{The Confidential LLM/RAG Pipeline}
% Brief: ingest -> embed -> store -> retrieve -> rerank -> generate, and where
% each stage leaks. RAG is the MOTIVATING FRAME here (scope decision); the
% measured contributions are inference-side.
\needswork{pipeline figure + per-stage leakage summary}

\subsection{Cryptographic Approaches (FHE / MPC / PIR)}
\needswork{FHE/MPC/PIR for inference and retrieval; overhead reality}

\subsection{Trusted Execution Environments}
% TEE / CVM (TDX, SEV-SNP, H100 CC), attestation, RATLS. TwinShield
% \cite{xue_twinshield} as a unified TEE+accelerator point.
\needswork{TEE family; near-plaintext overhead; historical vulnerabilities}

\subsection{Static Obfuscation}
% AloePri \cite{lin_aloepri} (covariant obfuscation), GELO \cite{belikov_gelo}.
% No TEE required; trades defense vs accuracy; limited against certain attacks.
\needswork{static obfuscation family; the defense/accuracy tension}

\subsection{Differential Privacy}
% DistanceDP for queries (RemoteRAG \cite{cheng2025remoterag}); epsilon tradeoff.
\needswork{DP for query/access-pattern; epsilon-recall tradeoff}

\subsection{Hybrid Split (TEE + Obfuscation)}
% The class this paper measures: boundary layers in TEE, bulk linear projections
% on an untrusted GPU over obfuscated activations.
\needswork{hybrid split designs; positions Sec. 4}

\begin{table*}[t]
  \centering
  \caption{Trust-mechanism families for confidential LLM/RAG (survey summary).}
  \label{tab:mechanisms}
  \begin{tabular}{@{}lllll@{}}
    \toprule
    Mechanism            & Trust assumption        & Overhead         & Quality impact   & Key refs \\
    \midrule
    Cryptographic        & none (math)             & \needswork{}     & \needswork{}     & \needswork{} \\
    TEE / CVM            & hardware vendor         & 4--10\%          & none             & \cite{xue_twinshield} \\
    Static obfuscation   & learned coverage        & low              & accuracy trade   & \cite{lin_aloepri,belikov_gelo} \\
    Differential privacy & noise calibration       & low              & recall trade     & \cite{cheng2025remoterag} \\
    Hybrid split         & TEE seal + obfuscation  & \needswork{}     & \needswork{}     & \cite{belikov_gelo} \\
    \bottomrule
  \end{tabular}
\end{table*}
