\section{Evaluation Framework: Security and Cost}
\label{sec:framework}

% UNIFIED framework section (grill #5): ONE framing for how we compare schemes, split into
% Security (categorical taxonomy) and Cost (quantitative perf+fidelity). The categorical/
% quantitative split is stated ONCE here; nothing downstream re-derives it. Side-channel
% finding lives in §\ref{sec:synthesis}; the validation case study in \cref{app:validation}.

We compare confidential Transformer-inference and RAG schemes along two kinds of
dimension: \emph{security}---categorical properties of who the adversary is and what
stays hidden (\cref{sec:threat})---and \emph{cost}---the quantitative price of that
protection, in performance and fidelity (\cref{sec:comparison}). This single split runs
through the paper: the security dimensions yield the threat-model-fit criterion of our
deployment-readiness lens (\cref{sec:intro}). We also fix here how schemes are
\emph{grouped} for comparison---by use case, not by family.

\subsection{Taxonomy: Threat Model and Security}
\label{sec:threat}

Confidential Transformer-inference and RAG systems are not built against a single
adversary. Each scheme makes its own assumptions about who the attacker is, what it
knows, what it can observe, and what must stay hidden---and those assumptions, not the
mechanisms alone, decide whether a defence is adequate. We classify the security of each
scheme along five \emph{categorical} axes (\cref{sec:threat-axes}), place a
representative set of schemes on them (\cref{sec:threat-landscape}), and treat the
attacker's \emph{capability budget} as a cross-cutting concern (\cref{sec:threat-budget}).
A field-wide side-channel blind spot emerges from the exercise (\cref{ssec:sidechannel}),
and the one empirical case study in this SoK (\cref{app:validation}) instantiates a single
point in this space.

\begin{table*}[t!]
  \centering
  \footnotesize
  \caption{Threat models across confidential Transformer-inference and RAG
    schemes, on the five axes of \cref{sec:threat-axes}. \emph{Knows}:
    wb=white-box weights, +s=+scheme. \emph{Protects} and \emph{Residual leak}
    are the two facets of confidentiality scope (asset hidden vs.\ what the server
    still sees): act=activations (VRAM), ap=access patterns, ct+dist=ciphertext +
    cosine-distance structure (the distance, not the ciphertext, is the leak). \emph{Basis}=security basis (crypto=cryptographic,
    DP=differential privacy, HW=hardware-enforced, heur.=heuristic;
    *=cryptographic but distance-leaking). Side channels are out of scope for
    every row (\cref{ssec:sidechannel}).}
  \label{tab:threat-taxonomy}
  \begin{tabular*}{\textwidth}{@{\extracolsep{\fill}}lllllll@{}}
    \toprule
    Scheme & Behavior & Trust anchor & Knows & Protects & Residual leak & Basis \\
    \midrule
    \multicolumn{7}{@{}l}{\textit{Static obfuscation}}\\
    AloePri      & HbC        & none           & wb+s & in+out      & obf.\ I/O+wts & heur. \\
    STIP         & HbC        & none (3-party) & wb   & input       & activations   & heur. \\
    \midrule
    \multicolumn{7}{@{}l}{\textit{TEE / hybrid}}\\
    GELO         & HbC        & GPU-TEE        & wb   & input       & activations   & heur.+HW \\
    ObfuscaTune  & HbC        & CPU/GPU-TEE    & wb   & data+model  & activations   & heur.+HW \\
    TwinShield   & HbC+verif. & CPU-TEE + SS   & wb   & data+model  & activations   & crypto+HW \\
    Opal         & malicious  & TDX + GPU-TEE  & ---  & data+access & op-type only  & crypto+HW \\
    PipeLLM      & HbC        & GPU-TEE        & ---  & data+model  & PCIe act.     & HW \\
    \midrule
    \multicolumn{7}{@{}l}{\textit{Cryptographic / DP / RAG}}\\
    PermLLM      & HbC        & 2PC+dealer     & wb   & input        & shares       & crypto+heur. \\
    RemoteRAG    & HbC        & none           & ---  & query        & query+ap     & DP \\
    Caprise      & HbC        & none           & ---  & doc+query    & ct+dist      & crypto*+DP \\
    Pacmann      & HbC        & none; clt.-relied & ---  & query+access & none (pub.\ DB) & crypto \\
    Compass      & malicious  & none; clt.-relied & --- & query+access & op-type only  & crypto \\
    \bottomrule
  \end{tabular*}
\end{table*}

\subsubsection{Axes}
\label{sec:threat-axes}

We classify each scheme along five axes.

\paragraph{1. Adversary behavior.}
\emph{Honest-but-curious} (HbC): the operator runs the protocol faithfully but mines every
observable for private information---the standard cloud model, adopted by most schemes here.
\emph{Malicious/active}: the operator may additionally deviate (drop, replay, reorder, tamper),
requiring integrity machinery on top of confidentiality. Behavior decides whether
confidentiality alone suffices.

\paragraph{2. Trust anchor (TCB).}
What the scheme roots its guarantees in: \emph{none} (purely cryptographic, statistical, or
obfuscation-based); a \emph{CPU-TEE} (Intel TDX, AMD SEV-SNP); a \emph{GPU-TEE} (a
confidential H100/H200/B200-class accelerator); or \emph{multi-party non-collusion} (the MPC
assumption). A larger TCB buys performance or simplicity at the price of a stronger trust
assumption. These anchors collapse onto a three-level \emph{hardware-trust} ladder we use to
organize the systematization and its scope (\cref{sec:intro}):
\begin{description}
  \item[Tier 1.] \emph{No trusted hardware}---confidentiality rests on cryptography or
        statistics alone (MPC/FHE, static obfuscation, DP). ``None'' constrains what must be
        \emph{trusted not to leak}, not where the \emph{work} runs: several Tier-1 schemes are
        not thin-client---client-side PIR/ORAM retrieval, or 2PC where the user is a compute
        party, make the client load-bearing, a deployment factor we revisit in
        \cref{sec:synthesis}.
  \item[Tier 2.] \emph{CPU-TEE only}---a commodity CPU enclave is trusted; any GPU is untrusted.
  \item[Tier 3.] \emph{Confidential accelerator}---a confidential GPU or accelerator-TEE trusted
        end-to-end.
\end{description}
The Tier-2$\to$Tier-3 jump---trusting the accelerator silicon end-to-end---is the sharpest
escalation on this axis, so a scheme's tier is the single most consequential entry in its row;
the hybrid split's defining move (\cref{sec:hybrid}) is to deliver Tier-3-class GPU performance
at Tier~2.

\paragraph{3. Attacker knowledge.}
\emph{Black-box} (queries only), \emph{white-box on weights} (knows the public model---the
realistic case for open-weight Transformers), or \emph{white-box plus the obfuscation scheme}
(knows the algorithm, ignorant only of an ephemeral per-session secret). We adopt Kerckhoffs's
principle throughout: the adversary knows the model and method and is denied only the fresh
secret. This is the honest assumption for open-weight deployments, and the regime in which
static schemes are most stressed---a transform secure only while the weights are private
collapses once an attacker who knows the public weights can solve for it (\cref{sec:obfuscation}).

\paragraph{4. Confidentiality scope.}
What the scheme keeps hidden and what it concedes---two faces of one question. The protected
\emph{asset} may be the input, the input \emph{and} output, the model weights, or the access
pattern over a corpus. Its dual is the \emph{residual leakage}---what the adversary still
observes despite the scheme (obfuscated I/O, mixed activations in untrusted VRAM, bounded
access patterns, or distance structure). Microarchitectural, cache, timing, and power side
channels are a residual category \emph{every} surveyed scheme places out of scope---a
uniformity we elevate to a finding (\cref{ssec:sidechannel}) rather than carry as a per-scheme
cell. Two schemes agreeing on every other axis can diverge sharply here, and as the case study
(\cref{app:validation}) shows, it is the residual leakage---not the stated protection
goal---that an attack ultimately exploits.

\paragraph{5. Security basis.}
What the guarantee \emph{rests on}, independent of \emph{which} entity is trusted (axis~2):
\emph{cryptographic} (provable under a hardness assumption, as in FHE/MPC under LWE/OT);
\emph{information-theoretic} (no computational assumption, as in one-time masking);
\emph{differential privacy} (a formal $(\varepsilon,\delta)$ or metric bound);
\emph{hardware-enforced} (reduces to TEE isolation and attestation); and \emph{heuristic}
(resistance shown only experimentally, as in most static obfuscation). This axis separates
schemes the others cannot: an FHE scheme and a static-obfuscation scheme can share an identical
row on axes~1--4 yet differ as cryptographic indistinguishability versus an empirically
measured resistance the case study (\cref{app:validation}) shows can be far weaker than
reported. Security basis is thus the sharpest predictor of whether a confidentiality claim
survives scrutiny, and the axis that \emph{threat-model fit} (\cref{sec:intro}) reads most heavily.

\subsubsection{The Landscape}
\label{sec:threat-landscape}

\cref{tab:threat-taxonomy} places a representative scheme from each of the five
mechanism families on the five axes; the full per-family rosters appear in
\cref{sec:crypto}--\cref{sec:hybrid}. Four patterns stand out. First, the
obfuscation family clusters tightly at \emph{HbC + white-box}: these schemes
explicitly assume the attacker knows the open-source model and the method, and
stake everything on an ephemeral secret. Second, the trust anchor varies more
than the adversary behavior does: schemes that face nearly the same attacker still
root trust in very different places---nothing, a CPU-TEE, or a GPU-TEE---which is the real
axis of disagreement in the field. Third, only Opal and Compass
defend a \emph{malicious} operator---Opal a host that may tamper with computation outside
the enclave, Compass a retrieval server that may deviate from the agreed search protocol
(caught by its commitment and integrity checks, with no trusted hardware); every other
scheme assumes the operator follows the protocol. Fourth---and most consequential---the \emph{security basis} cleaves the
table where no other axis can: the obfuscation rows rest on heuristic,
experimentally-measured resistance, while PermLLM and the cryptographic-retrieval
schemes rest on hardness assumptions and the TEE designs on hardware. Several
heuristic and cryptographic schemes alike anchor trust in \emph{nothing}
(trust anchor \emph{none}), so this distinction is invisible without the basis
axis---yet it is exactly what the case study (\cref{app:validation}) turns on.

\needswork{rows are grounded via the EdgeQuake-indexed source PDFs (AloePri, GELO,
ObfuscaTune, TwinShield, Opal, PermLLM, RemoteRAG, Caprise, Pacmann, Compass);
PipeLLM/STIP from their preprints --- re-confirm at submission}


% Shared inference-performance table at the start of §2.2; each inference family
% (\cref{sec:crypto}--\cref{sec:hybrid}) contributes a band.
\begin{table*}[t!]
  \centering \scriptsize\setlength{\tabcolsep}{4pt}
  \caption{\textbf{Inference performance across mechanism families} (exemplars; the shared
    inference cost table, replacing the former per-family tables). Rows are grouped by family.
    Cryptographic rows give \emph{absolute} latency---per generated token (``/tok'', decoders) or
    per 128-token forward pass (``/inf'', encoders)---and report no vs-plaintext multiplier; TEE,
    obfuscation, DP, and hybrid rows report \emph{$\times$-overhead} vs.\ plaintext on the same
    hardware (their communication is local/negligible and omitted). Figures are paper-reported except $\dagger$ =
    extrapolated 1-hop via the cited baseline (\cref{sec:comparison}); cross-row figures are
    indicative, not co-measured (\cref{sec:comparison}). The Fidelity column is the accuracy/utility
    delta vs.\ plaintext; obfuscation/DP privacy claims are discussed in their sections
    (\cref{sec:obfuscation},~\cref{sec:dp}) and tested in \cref{app:validation}.}
  \label{tab:inf-perf}
  \begin{tabular*}{\textwidth}{@{\extracolsep{\fill}}lllll@{}}
    \toprule
    Scheme & Technique & Model & Latency / overhead & Fidelity ($\Delta$acc) \\
    \midrule
    \multicolumn{5}{@{}l}{\emph{Cryptographic (MPC/FHE)} --- \cref{sec:crypto}}\\
    SHAFT~\cite{shaft}        & 2PC               & BERT-base (110M)        & ${\approx}1.4$\,s/inf$^\dagger$; ${\approx}0.6$\,GB/inf$^\dagger$ & ${\approx}0$ \\
    Fission~\cite{fission}    & MPC+evaluators    & ModernBERT (149M)       & $<$5\,s/inf; $<$3\,GB/inf                          & ${\approx}0$ \\
    Euston~\cite{euston}      & FHE (non-int.)    & BERT-base (110M)        & ${\approx}10.7$\,s/inf$^\dagger$; ${\approx}37$\,MB/inf$^\dagger$ & approx.$^\dagger$ \\
    PermLLM~\cite{permllm}    & 2PC+dealer        & ChatGLM-6B              & 3\,s/tok; 20\,Mb/tok                            & exact (0) \\
    CryptoMoE~\cite{cryptomoe}& 2PC (MoE)         & OLMoE-7B-A1B & 22\,s/tok; 2.8\,GB/tok             & $-0.8\%$ \\
    \midrule
    \multicolumn{5}{@{}l}{\emph{TEE (Tier 3, baseline)} --- \cref{sec:tee}}\\
    H100 CC~\cite{h100cc}     & GPU-TEE (H100 CC) & Llama2 7--70B           & $1.04$--$1.08\times$                            & exact \\
    PipeLLM~\cite{pipellm}    & GPU-TEE+CVM       & OPT 13--175B            & ${\approx}1.2\times$ ($<$19.6\%)                & exact \\
    \midrule
    \multicolumn{5}{@{}l}{\emph{Static obfuscation} --- \cref{sec:obfuscation}}\\
    AloePri~\cite{lin_aloepri}& covariant data+weight & Qwen/Llama $\le$671B & ${\approx}1\times$                              & 0--3.5\% acc.\ loss \\
    SGT~\cite{roberts_sgt}    & learned embed.\ transform & Llama-1B/Qwen3   & ${\approx}1\times$ ($\approx$0\,ms)             & $-0.4\%$ util \\
    OSNIP~\cite{cao_osnip}    & semantic null-space & Llama-3.2/Qwen3       & ${\approx}1\times$ (0.96\,ms)                   & near-lossless \\
    Eguard~\cite{eguard}      & MI-loss projector & T5/MPNet/RoBERTa        & ${\approx}1.7\times$                            & ${>}98\%$ task acc \\
    KV-Cloak~\cite{luo_shadowcache} & KV-cache per-block perm & LLaMA-3.1-8B    & ${\approx}1\times$ ($<$0.45\% prefill)          & lossless \\
    ObfusLM~\cite{obfuslm}    & $(k,\varepsilon)$-anon embed+head & BERT/Llama3-8B & ${\approx}1\times$ (1.29\,s/inf)             & $-4\%$ clf / $-6\%$ gen \\
    CodeCipher~\cite{codecipher} & token confusion map (code) & CodeLlama-7B   & ${\approx}1\times$ (offline opt.)               & $-3$\,pp Pass@1 \\
    EE~\cite{buban_ee}        & equivariant layer transform & generic LLM/CNN & ${\approx}1\times$ (near-zero)                  & exact \\
    \midrule
    \multicolumn{5}{@{}l}{\emph{Differential privacy} --- \cref{sec:dp}}\\
    DP-Forward~\cite{du_dpforward} & matrix-Gaussian fwd & BERT enc.         & ${\approx}1\times$                              & ${\approx}94\%$ SST2 \\
    SPARSE~\cite{sparse_dp}   & metric-DP mask    & GTR/SBERT enc.          & ${\approx}1\times$                              & 65\% util @$\varepsilon{=}5$ \\
    DP-KSA~\cite{tang_dprag}  & output-DP         & Qwen2.5-3B/Llama        & ${\approx}N\times$ gen.\ calls                  & F1 $+3.5$ @$\varepsilon{=}8$ \\
    \midrule
    \multicolumn{5}{@{}l}{\emph{Hybrid split (Tier 2)} --- \cref{sec:hybrid}}\\
    GELO~\cite{belikov_gelo}  & conf.\,GPU + per-batch mix & Llama-2-7B        & $1.2$--$1.3\times$ (76\% offl., microbench.)    & exact \\
    TwinShield~\cite{xue_twinshield} & CPU-TEE + SS + U-Verify & BERT/ViT/LLaMA & $4$--$6\times$ vs TEE-only                   & exact \\
    ObfuscaTune~\cite{frikha_obfuscatune} & ${\sim}5\%$ params in TEE & GPT-2 & $1.5$--$4.3\times$                             & exact-ish \\
    SCX~\cite{zeng_scx}       & per-session OTK KV-cache & 7B decoder       & ${\approx}1\times$ (online)                     & exact \\
    CMIF~\cite{cmif}          & CPU-TEE embed + RNM-DP & Llama2/3 7--8B      & $1.3$--$1.4\times$                              & DP utility cost \\
    \bottomrule
  \end{tabular*}

  \smallskip
  {\footnotesize $^\dagger$ extrapolated 1-hop (\cref{sec:comparison}): SHAFT via SIGMA~\cite{sigma}
  (1.84\,s/0.99\,GB, BERT-base, 128 tok); Euston via NEXUS~\cite{nexus} (37.3\,s/164\,MB, BERT-base,
  128 tok; NEXUS $\Delta$acc $0.16$--$0.38\%$ per Li et al.). SHAFT's own reported BERT-base time
  is 41\,s LAN; the $\dagger$ figures recover a GPU-class absolute, not a vs-plaintext ratio.}
\end{table*}


\subsubsection{The Attacker's Capability Budget}
\label{sec:threat-budget}

The five axes say what an adversary \emph{is}, not how \emph{much} it can spend---yet the
capability budget (compute, and especially the observation horizon: a single inference vs.\
many adaptive runs) is decisive, because the surveyed schemes target markedly different
budgets. AloePri is explicit that it ``aims to provide adjustable security for constrained attackers in
real-world scenarios, rather than offering ideal-world security guarantees against worst-case
attackers'' \citep{lin_aloepri}; GELO's
guarantee is per-batch non-identifiability under a fresh mixing matrix, conceding nothing
\emph{across} batches but leaving a single-batch blind-source-separation problem
\citep{belikov_gelo}. A worst-case adversary with unbounded compute and unlimited adaptive
observation defeats every static-obfuscation scheme here; an evaluation assuming one would
measure nothing interesting and strawman schemes that never claimed that bar. This is why
\emph{threat-model fit}---not maximality---is a deployment-readiness criterion: a scheme
defending an adversary stronger than its deployment faces overpays in performance, one
defending a weaker adversary fails to protect, and the deployable point is the calibrated one,
on which we score families in \cref{sec:synthesis}.

We therefore treat the budget as an explicit per-scheme parameter. The ``what breaks'' case
study (\cref{app:validation}) accordingly attacks each scheme \emph{within its own
constrained-attacker regime}---white-box on weights and method, denied the per-session secret,
under the compute and observation horizon the scheme's own evaluation permits---so a break
there is a break by the victim's own standard, not an artefact of an inflated adversary.

\subsection{Cost: Performance and Fidelity}
\label{sec:comparison}

The other two deployment-readiness criteria are quantitative: \emph{performance}
(overhead and latency) and \emph{fidelity} (impact on output quality). Comparing them
across papers is hard to do fairly: reported numbers come from different hardware, model
sizes, datasets, and metrics, so a raw side-by-side can mislead. We fix here a common set of
\emph{reference metrics} for both, and state how we group schemes when we compare them.

\paragraph{Normalizing performance.}
Wherever a paper gives a plaintext baseline we express cost as an \emph{overhead multiplier}
($\times$ vs.\ plaintext on the same hardware and model); otherwise we compare only \emph{within
a regime}---a (model-size $\times$ hardware) bucket (e.g.\ BERT-base on one GPU; a 7B decoder
over WAN)---never across regimes. Cross-scheme figures are \emph{indicative} by default (each
from its own testbed: an order of magnitude, not a precise ratio); only figures we flag
\emph{co-measured} (benchmarked together in one paper) support direct comparison. Even within a
regime, a comparison is fair only when the number of parties, the numerical precision, and the
network setting match---Chen et al.~\citep{chen_sok_pti} find WAN-tuned schemes miss their
reported numbers under other network profiles, so LAN and WAN figures are not interchangeable.

\paragraph{One-hop relative normalization.}
Cryptographic-inference papers report cost \emph{relative to a prior secure scheme}, not
plaintext. Where a scheme $A$ reports a $k\times$ factor over a baseline $B$ that gives an
absolute figure in the \emph{same} regime, we recover $A$'s absolute cost as
${\approx}\,\mathrm{cost}(B)/k$ and flag it \emph{extrapolated} (1-hop); we never chain further
or extrapolate across regimes. Since these chains terminate at secure schemes rather than
plaintext, a vs-plaintext multiplier for crypto inference is only an order-of-magnitude
estimate, flagged as such.

\paragraph{Normalizing fidelity.}
Schemes that compute the true function (cryptography, TEE, invertible-mask hybrid splits) are
\emph{exact}; approximate schemes (obfuscation, DP, polynomial-approximation MPC) are reported
as a \emph{delta} against the plaintext baseline in the use-case-appropriate metric, so a
retrieval recall drop and a generation accuracy drop are never conflated.

\paragraph{Grouping: by use case, not by family.}
We \emph{systematize} by mechanism family (\cref{sec:crypto}--\cref{sec:hybrid}) but
\emph{compare} performance and fidelity \textbf{by use case}, since a deployment decision weighs
every mechanism that serves a stage against the others (private generation pits MPC, TEE,
obfuscation, and hybrid split against each other, not FHE against obfuscation in the abstract).
We use four use cases aligned with the gap-map stages (\cref{sec:synthesis}): \emph{generation}
(decoder) and \emph{embedding} (encoder) inference, and retrieval split by index structure into
\emph{vector} (flat/IVF ANN and vector storage) and \emph{graph} (graph-based ANN and
graph-semantic search), which impose different costs and threat surfaces; each scheme is grouped
by the model class it evaluates. \emph{Reranking} has no standalone private measurement in the
corpus, so it stays an open gap (\cref{sec:synthesis}), not a cost bucket. Each use case has a
canonical metric: latency and $\times$-overhead for generation and embedding; latency/query and
communication at a fixed corpus size for retrieval; and fidelity as the task-accuracy
(generation/embedding) or recall@$k$ (retrieval) delta against the plaintext baseline.

% [tab:inf-perf moved to the START of §2.2 (below) so it lands before §3 in two-column flow.]
