\section{Conclusion and Future Work}
\label{sec:conclusion}

% Restate the holds/breaks finding, bounded by the threat model. Then future work:
%   - v2: build + measure the unified static+dynamic design (Sec. 8).
%   - GraphRAG / encrypted semantic search (Compass).
%   - out-of-scope threats: active adversary, microarch side-channels.
\needswork{conclusion; tie back to the headline; list future work}
