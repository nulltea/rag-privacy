\section{At-Rest Private Storage: Caprise}
\label{sec:caprise}

% LOCKED scope: RAG = motivating frame + survey; ONE real RAG measurement, the
% Caprise reproduction, to keep "RAG" in the title honest. MAY MOVE TO APPENDIX
% if the body runs long. Status: NOT YET RUN (cheap; ~2339 vec/s reported).

\subsection{Caprise in Brief}
% Distance-preserving encryption for vector storage \cite{ye_caprise}: similarity
% search runs on ciphertexts; what it protects (content recovery) and leaks
% (inter-embedding distance structure).
\needswork{summarize Caprise}

\subsection{Reproduction}
% Reproduce throughput + the Vec2Text-resistance result (BLEU drop). Tolerance per
% the replication protocol.
\begin{table}[t]
  \centering
  \caption{Caprise: reproduced vs.\ claimed.}
  \label{tab:caprise-repro}
  \begin{tabular}{@{}lrr@{}}
    \toprule
    Metric & Claimed & Reproduced \\
    \midrule
    Encryption throughput (vec/s) & \needswork{} & \needswork{} \\
    Vec2Text BLEU (pre/post)      & \needswork{} & \needswork{} \\
    Retrieval recall              & \needswork{} & \needswork{} \\
    \bottomrule
  \end{tabular}
\end{table}
\needswork{run Caprise repro; fill table; note the distance-structure leakage caveat}
