% ABSTRACT — write last. ~150-200 words. Mirror the contributions list in the intro.
% Beats to hit (in order):
%   1. Setting: outsourced confidential LLM inference + RAG to an untrusted cloud.
%   2. Gap: prior obfuscation/TEE-split schemes (AloePri, GELO, TwinShield) report
%      narrow evaluations; the realistic attack surface is under-reported.
%   3. What we do: SoK by trust mechanism + a like-for-like measurement study on one
%      substrate under a fixed semi-honest threat model.
%   4. Headline finding: static obfuscation (AloePri) BREAKS under attack classes its
%      authors never ran (inversion / GRAM / ISA AttnScore); a TEE+untrusted-GPU split
%      HOLDS against the same suite at a measured overhead of \needswork{X\%}.
%   5. Plus: first systematic re-evaluation of AloePri; Caprise at-rest storage repro.
%   6. Framing: we sketch a unified static+dynamic design that the evidence motivates
%      (full evaluation is future work).

\needswork{draft abstract last, once the numbers in \cref{sec:eval} are final}
