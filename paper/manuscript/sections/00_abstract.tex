% ABSTRACT — write last. ~200 words. SoK framing.
% Beats:
%   1. Setting: confidential Transformer inference (embedding/reranking/generation)
%      + RAG outsourced to an untrusted cloud.
%   2. State: many point solutions (crypto, TEE, obfuscation, DP, hybrid split);
%      no systematization unifies mechanism, threat model, AND performance, and
%      the existing RAG-privacy SoK (Bodea et al.) is risk-centric, naive-RAG,
%      and omits a performance axis.
%   3. What we do: systematize the field by defense mechanism, under a 6-axis
%      threat-model taxonomy, with a performance comparison synthesized from
%      reported numbers; spanning LLM inference AND RAG including graph-RAG.
%   4. Artifacts: the threat-model taxonomy; a stage x mechanism gap-map; a
%      normalized performance comparison.
%   5. Findings: e.g. the field-wide side-channel blind spot; the under-reported
%      vulnerability of static obfuscation (vignette); the empty gap-map cells.
%   6. Contribution framing: complements Bodea (risk side); fills its named gaps
%      (practical cost, advanced/graph-RAG architectures, crypto/TEE depth).
\needswork{draft abstract last, once §8 synthesis numbers are final}
