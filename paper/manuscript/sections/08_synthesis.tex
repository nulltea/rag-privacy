\section{Synthesis: Coverage, Gaps, and Performance}
\label{sec:synthesis}

% The payoff section. Three artifacts: (1) a stage x mechanism matrix whose empty
% cells are the gap-map; (2) a normalized performance comparison from reported
% numbers + a non-comparability caveat; (3) graph-RAG as a retrieval setting.

\subsection{The Stage\,$\times$\,Mechanism Matrix and Gap-Map}
% Rows = pipeline stages (ingest, embed, store, retrieve, rerank, generate, +
% graph construction, graph traversal); columns = mechanism families (§3-§7).
% Cells = representative schemes. EMPTY / weak cells = open problems.
\begin{table*}[t]
  \centering \scriptsize
  \caption{Coverage of the inference-and-RAG pipeline by mechanism family. Cell = one
    or two representative schemes (\cref{sec:crypto}--\cref{sec:hybrid}; see those
    tables for the full set); ``---'' = no practical scheme known to us (a gap).}
  \label{tab:gapmap}
  \begin{tabular}{@{}llllll@{}}
    \toprule
    Pipeline stage & Crypto/MPC & TEE & Obfuscation & DP & Hybrid split \\
    \midrule
    Embed            & SecFormer/NEXUS    & H100-CC          & SGT/Eguard    & DP-Fwd/SPARSE & GELO/ObfuscaTune \\
    Store (vectors)  & CAPRISE/SAP        & Opal             & ---           & ---           & --- \\
    Retrieve (ANN)   & Compass/Pacmann    & Opal+ORAM        & ---           & RemoteRAG     & --- \\
    Rerank           & p\textsuperscript{2}RAG/TRSE & Opal   & ---           & RemoteRAG(HE) & --- \\
    Generate         & PermLLM/BumbleBee  & PipeLLM          & AloePri/OSNIP & DP-RAG(out)   & GELO/Amulet \\
    Graph construct. & ---                & ---              & ARoG          & ---           & PrivGemo \\
    Graph traverse   & Pacmann/XorMM      & Opal/H$_2$O$_2$RAM & ARoG        & ---           & PrivGemo \\
    \bottomrule
  \end{tabular}
\end{table*}
\needswork{narrate the headline gaps the ``---'' cells expose: obfuscation and DP are
almost absent from storage/retrieval/rerank; the hybrid split is absent from every
retrieval stage (it is an inference technique); graph \emph{construction} is a near-total
gap across all confidentiality families (only anonymization touches it). Integrity
(verification) is orthogonal to these columns --- ZKGraph/V3DB/ZKIFV compose on top
(\cref{sec:threat}). Note explicitly that secure GNN-inference / graph-analytics
schemes were excluded as off-target for retrieval-style graph-RAG.}

\subsection{Performance Comparison (from Reported Numbers)}
% COMPARABILITY CAVEAT (mandatory): these numbers are self-reported across
% different hardware, models, datasets, and metrics --- they are indicative, not
% head-to-head. Normalize where possible (overhead x vs plaintext; latency;
% comm); flag each as head-to-head vs self-reported.
\dnote{reviewers attack cross-paper performance tables; lead with the caveat and
mark every figure's provenance}
\begin{table*}[t]
  \centering \footnotesize
  \caption{Indicative performance from \emph{reported} numbers --- NOT
    head-to-head (different hardware/models/datasets). See caveat.}
  \label{tab:performance}
  \begin{tabular}{@{}lllll@{}}
    \toprule
    Mechanism family & Latency / overhead & Comm & Quality impact & Source basis \\
    \midrule
    Crypto/MPC    & 1--2 orders slower & high & none (exact) & self-reported \\
    TEE           & single-digit \%    & low  & none         & self-reported \\
    Obfuscation   & near-plaintext     & low  & accuracy trade & self-reported \\
    DP            & near-plaintext     & low  & recall trade & self-reported \\
    Hybrid split  & 20--30\% (GELO)    & med  & none (exact) & self-reported \\
    \bottomrule
  \end{tabular}
\end{table*}
\needswork{fill with specific cited figures; add the normalization method; this
performance axis is the dimension Bodea \cite{bodea_sok} explicitly omits}

\subsection{Graph-RAG as a Retrieval Setting}
% Woven treatment (decision: short dedicated subsection, not a parallel track).
% Graph-RAG (LightRAG, Microsoft GraphRAG) indexes a corpus as a knowledge graph;
% privacy work here is sparse (Ning et al. KG-QA privacy; Compass for encrypted
% search; Opal's in-enclave KG traversal). Bodea explicitly excludes this setting.
Graph-RAG systems such as LightRAG~\citep{guo_lightrag} index a corpus as a knowledge
graph; their retrieval surface is entity/relation ANN plus \emph{one-hop} neighborhood
expansion and a source-chunk fan-out---not multi-hop reasoning or GNN message-passing.
That shape determines which mechanisms apply: ANN matching is served by
Compass/Pacmann~\citep{zhu_compass,pacmann}, the distinctive adjacency/degree leak by
volume-hiding multi-maps (XorMM/FLASH)~\citep{xormm,flash_emm}, traversal-integrity by
ZKGraph~\citep{zkgraph}, and output-extraction (AGEA-class) by anonymization or
output-DP~\citep{ning_arog,tan_privgemo,tang_dprag}. Conversely, secure GNN-inference
and graph-analytics schemes are off-target for this setting and we cite them only as
lineage. Bodea et al.~\citep{bodea_sok} exclude graph-RAG entirely; the near-empty
graph rows of \cref{tab:gapmap} are the resulting open frontier.
\needswork{expand: per-stage attack-surface change vs naive RAG; the construction-stage gap}
