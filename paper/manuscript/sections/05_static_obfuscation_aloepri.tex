\section{Static Obfuscation (AloePri): What Breaks}
\label{sec:aloepri}

% LOCKED framing: KNOWN attack classes, FIRST instantiated & measured against
% AloePri. Claim = "first systematic evaluation of AloePri against the attack
% surface its authors omitted." Diligence cost: a per-attack citation map proving
% each attack's origin in prior work. Status: DONE (repro + numbers).

\subsection{AloePri in Brief}
% Covariant obfuscation \cite{lin_aloepri}: what it protects, what it reports.
\needswork{summarize AloePri's scheme + its reported evaluation}

\subsection{Replication}
% Reproduce their headline numbers. Report reproduced-vs-claimed with a tolerance.
\dnote{open decision: reproduction tolerance threshold — stage-1 still-open item \#2;
apply .claude/rules/replication-protocol.md}
\begin{table}[t]
  \centering
  \caption{AloePri: reproduced vs.\ claimed.}
  \label{tab:aloepri-repro}
  \begin{tabular}{@{}lrrr@{}}
    \toprule
    Metric & Claimed & Reproduced & $\Delta$ \\
    \midrule
    \needswork{metric 1} & \needswork{} & \needswork{} & \needswork{} \\
    \bottomrule
  \end{tabular}
\end{table}

\subsection{The Attack Surface AloePri's Evaluation Missed}
% IMPORTANT CORRECTION (from corpus grounding): AloePri ALREADY evaluates a broad
% suite -- VMA, IA (obfuscation recovery); ISA, IMA, NN (training-based inversion);
% TFMA, SDA (token-frequency) -- and reports <15% token recovery (TTRSR). So the
% claim is NOT "they ran no attacks." It is: within AloePri's OWN constrained-
% attacker regime (\cref{sec:threat}, capability budget), two attacks they did not
% run -- ArrowMatch and Sequence-IMA -- recover substantially more. The Gram-matrix
% leak is GELO's finding (orthogonal-mixing schemes), cite accordingly; it is NOT
% one of our headline attacks.
\dnote{frame as: AloePri evaluates VMA/IA/ISA/IMA/NN/TFMA/SDA at <15\% TTRSR; we
add ArrowMatch + Sequence-IMA, which it did not run, within its own threat model}
\begin{table}[t]
  \centering
  \caption{Token recovery (TTRSR) against AloePri: its reported suite vs.\ the
    attacks we add, all under AloePri's own constrained-attacker regime.}
  \label{tab:aloepri-attacks}
  \begin{tabular}{@{}lll@{}}
    \toprule
    Attack & Status & TTRSR vs.\ AloePri \\
    \midrule
    ISA / IMA / NN / VMA / IA & AloePri-reported & $<15\%$ (their claim) \\
    \needswork{ArrowMatch}    & \textbf{ours (new)} & \needswork{} \\
    \needswork{Sequence-IMA}  & \textbf{ours (new)} & \needswork{} \\
    \bottomrule
  \end{tabular}
\end{table}
\needswork{describe ArrowMatch + Sequence-IMA; pull results from the parent repo
evals(aloepri); state the budget = AloePri's own (constrained, white-box, scheme-
known) so the kill is fair (\cref{sec:threat})}
