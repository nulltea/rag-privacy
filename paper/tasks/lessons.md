- 2026-06-04 | address-comments reported "all 25 resolved" against the markdown count, not the PDF's true annotation total — silently ignored comments the md bucketed away | Reconcile extracted count vs `pdfannots -f json` length (=M); every comment gets a terminal status; conservation gate M==rows==Σstatuses; enumerate all in the report.
2026-06-05 | wrote handoff to /tmp (stale skill), then to repo-root /docs/handoffs — both wrong | Paper handoffs go to the CWD-LOCAL paper/docs/handoffs/YYYY-MM-DD-<slug>.md (working dir = paper/), NOT /tmp and NOT repo-root /docs (that's prototype/ML work). Always `ls docs/handoffs/` from cwd first. Skill + memory patched.
2026-06-05 | launched a background deep-research workflow then went idle ("I'll wait for a notification") — user had said not to fire-and-forget background agents | Do research/long tasks in the FOREGROUND, staying engaged and reporting progress as I go. Do NOT spawn background agents/workflows and then sit idle. If a task is big, drive it myself with parallel tool calls in-conversation, or only background it if the user explicitly asks.
2026-06-05 | new LaTeX table used bare p{} fixed-width columns → text justified, stretching inter-word spaces into ugly "padding" in narrow cells (recurring issue) | For ANY fixed-width p{} (or tabularx X) table column holding prose, ALWAYS prefix with >{\raggedright\arraybackslash} (array pkg, already loaded). Precedent: sections/09_synthesis.tex uses exactly this. Never ship a bare p{c} column with multi-word text.
2026-06-05 | first "fix" (raggedright) removed inter-word stretching but table still looked padded — fixed-width p{} columns summed to ~16cm < \textwidth (~17.8cm), so the table sat INSET and booktabs rules stopped short of the caption | For prose tables, use tabularx{\textwidth} with >{\raggedright\arraybackslash}X for the wide columns (fixed p{} only for short ones). This fills \textwidth exactly (rules reach caption edges, slack absorbed into columns) AND stays ragged-right (no justification). Two failure modes to check together: (a) justification padding [needs raggedright], (b) inset/narrow-table padding [needs tabularx{\textwidth} or tabular*+extracolsep]. Verify by eye, not just build-clean.
2026-06-05 | tabularx table still illegible: every row wrapped to 2 lines with NO vertical gap, so adjacent rows blended — couldn't tell where one entry started/ended | When ANY booktabs row wraps to multiple lines, separate rows with \addlinespace[5pt] between them (+ \renewcommand{\arraystretch}{1.15}). Rule of thumb: if cells wrap, rows MUST be visually grouped. Full prose-table recipe now: tabularx{\textwidth} + >{\raggedright\arraybackslash}X + \addlinespace between multi-line rows + arraystretch ~1.15. THREE failure modes seen in sequence today (justification, inset, row-blend) — check all three in the PDF.
2026-06-05 | REPEATED the inset-padding bug on a single-column round-trip table despite the lesson — root cause: lesson said fill "\textwidth", but a single-column `table` float must fill `\columnwidth` (\textwidth = both-column width). I'd used a bare tabular with narrow fixed cols → inset, rules short of edges. | Match width to float type: `table*`→\textwidth, `table`→\columnwidth. Numeric tables: `\begin{tabular*}{<width>}{@{\extracolsep{\fill}}...}`. Prose: tabularx{<width>}. NEVER bare tabular with fixed cols < target width. Memory feedback-latex-table-padding.md updated with the float-type distinction.
2026-06-06 | when fixing a mistake the user flagged (re-attributing the Gram/ICA-BSS attack away from GELO), I wrote the CORRECTION RATIONALE into the artifact — "(attack family from classical BSS; evaluated against GELO)" in a §7 table cell and "Origin of the attack family, not GELO" in a bib note. User has had to clean this up many times. | HARD RULE: never narrate a correction inside the document (prose, captions, bib `note`, comments). Change the wording/citation to the correct version and STOP; the fix must read as if it was always correct. Put the why (what was wrong, the source, the reattribution) ONLY in the chat reply. No "not X", "originally Y", "actually", "corrected:", defensive justifications. Bib notes = neutral bibliographic descriptors only. Memory: feedback-no-correction-commentary.md.
